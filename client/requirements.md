## Packages
framer-motion | Smooth animations for form interactions and page transitions
lucide-react | Icons for UI elements

## Notes
The backend returns a raw PDF buffer/blob. The frontend must handle the binary response and create a download link object URL.
BS Date conversion logic happens on the backend; frontend just sends the raw BS date components.
